info:
  name: get-user-logbook
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/users/{{user_id}}/logbook?types=ascent,bid"
  headers:
    - name: Cookie
      value: "{{token}}"
  params:
    - name: types
      value: ascent,bid
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
