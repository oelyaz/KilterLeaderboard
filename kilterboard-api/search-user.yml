info:
  name: search-user
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/explore?q=schorsch007&t=user"
  headers:
    - name: Cookie
      value: "{{token}}"
  params:
    - name: q
      value: schorsch007
      type: query
    - name: t
      value: user
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
