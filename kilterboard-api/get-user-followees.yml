info:
  name: get-user-followees
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/users/{{user_id}}/followees"
  headers:
    - name: Cookie
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
