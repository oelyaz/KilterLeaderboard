info:
  name: get-user-followers
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/users/{{user_id}}/followers"
  headers:
    - name: Cookie
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
