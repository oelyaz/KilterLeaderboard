info:
  name: get-user
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/users/{{user_id}}"
  headers:
    - name: Cookie
      value: "{{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
